import { TrucanteTextPipe } from './trucante-text.pipe';

describe('TrucanteTextPipe', () => {
  it('create an instance', () => {
    const pipe = new TrucanteTextPipe();
    expect(pipe).toBeTruthy();
  });
});
